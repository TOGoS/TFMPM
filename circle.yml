machine:
  php:
    version: 5.5.9

database:
  override:
    - cp config/dbc.json.circle config/dbc.json
    - make build/db/create-database.sql
    - psql -U ubuntu -v ON_ERROR_STOP=1 circle_test <build/db/enable-extensions.sql
    - touch .database-created
    - util/test-db-connection

test:
  override:
    - make run-unit-tests
